networks:
  traefik:
    external: true

services:
  trek:
    container_name: trek
    image: docker.io/mauriceboe/trek:3.3.0@sha256:1abd6fc1fcb336aeb2be38552fb5f0a4fd9c3ef629b609a2dd99c8cc9954942b
    restart: unless-stopped
    read_only: true
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:noexec,nosuid,size=128m
    volumes:
      - /opt/data/trek/data:/app/data:z
      - /opt/data/trek/uploads:/app/uploads:z
    env_file:
      - /opt/secrets/trek.env
    environment:
      NODE_ENV: production
      PORT: 3000
      TZ: Europe/London
      LOG_LEVEL: info
      APP_URL: https://${TREK_HOST}
      ALLOWED_ORIGINS: https://${TREK_HOST}
      TRUST_PROXY: "1"
      OIDC_ONLY: "true"
      OIDC_DISPLAY_NAME: Microsoft
      OIDC_SCOPE: openid email profile
      ALLOW_INTERNAL_NETWORK: "false"
    networks:
      - traefik
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
